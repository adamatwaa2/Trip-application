<section class="pi-section pi-section--ivory">
  <div class="pi-container pi-container--read">
    <p class="pi-eyebrow">404</p>
    <h1 class="pi-listing-hero__title">That page isn't here</h1>
    <p class="pi-listing-hero__lede">The link may be old, or the page may have moved. Everything we run is one click away.</p>
    <div class="pi-cta__actions" style="justify-content:flex-start;margin-top:var(--pi-s4)">
      <a href="{{ routes.root_url }}" class="pi-btn pi-btn--primary">Back to home</a>
      <a href="{{ routes.collections_url }}" class="pi-btn pi-btn--secondary">Explore</a>
    </div>
  </div>
</section>
